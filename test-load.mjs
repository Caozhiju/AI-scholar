import { stages, type Stage, type Course } from "./src/data/courses.js"
console.log("stages loaded, length:", stages.length)
console.log("First stage:", stages[0].id)
console.log("Last stage:", stages[stages.length - 1].id)
